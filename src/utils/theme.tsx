import { createTheme } from "@material-ui/core/styles";

const primaryColor = "";
const secondaryColor = "";
export default createTheme({
  palette: {
    primary: {
      main: `${primaryColor}`,
    },
    secondary: {
      main: `${secondaryColor}`,
    },
  },
  
});
